{
    "name": "tf-toxicity-webext",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "node esbuild.js",
        "start": "node esbuild.js && web-ext run --source-dir=dist --start-url=https://example.com"
    },
    "devDependencies": {
        "esbuild": "^0.19.3",
        "typescript": "^5.2.2",
        "web-ext": "^7.7.0"
    },
    "dependencies": {
        "@tensorflow-models/toxicity": "^1.2.2"
    }
}
